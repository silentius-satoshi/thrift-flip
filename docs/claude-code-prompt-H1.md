# Claude Code Prompt — H1: Live-Check Harness
<!-- Model: Opus 5 · Effort: Max thinking · Verified against repo @ cbebe5e (V1+S1 complete) -->

Add `scripts/live-check.mjs`: a zero-dependency Node script that automates the automatable half of `docs/v1-live-check-runbook.md` and runs the **grounded-vs-ungrounded pricing experiment** whose result decides comps tier A (plan §6, H1 row). Keys come from the environment only — `GEMINI_KEY` required, `SERPAPI_KEY` optional — never from files, never committed. Fixtures live in `fixtures/live/` (**gitignore it**).

- **Fixture convention:** `fixtures/live/<slug>__<expected-brand>-<expected-model>__<n>.jpg` — e.g. `sneaker__nike-airmax90__1.jpg`, up to 3 photos per slug. Core slugs for the gate: `sneaker`, `book`, `tool`, `mug`, `electronics`.
- **Per item (ungrounded arm):** call the same path the app uses — import `analyzeItem` from `src/utils/ai.js` with the env key injected via the same storage seam (do not fork the prompt/schema/config; zero drift from production). Score `identification.brand`+`model` against the filename labels (case-insensitive substring → correct / partial / wrong); record `pricing.estimate`, range, `confidence`.
- **Grounded comparison arm (experiment):** for each item, a second call with Google Search grounding (`tools: [{ google_search: {} }]`). If grounding + `responseSchema` cannot combine in one request (**verify against current API docs — do not assume either way**), two-step: grounded free-text price query → structured formatting call. Record grounded estimate/range alongside ungrounded. **Measurement only — do not change `src/utils/ai.js`'s default path**; whether grounding becomes production default is a plan-level decision taken after this data exists. Report grounded-query cost/credit consumption.
- **Sold-price ground truth (optional):** if `SERPAPI_KEY` is set, query SerpApi's eBay engine per item (`engine=ebay`, `_nkw=<expected brand + model>`, `LH_Sold=1`, `LH_Complete=1`), compute median + range of sold prices, auto-fill the ground-truth column marked `via SerpApi sold data`; score BOTH arms' estimates as inside/outside the sold range. Absent key or empty result → leave `⟵ fill from eBay sold filter` placeholder. Note credits consumed.
- **Anchoring test:** for `--anchor=<slug>`, run the ungrounded arm twice — `Goodwill price` $4 vs $30, identical photos/notes. Report the estimate delta; >10% movement → print `ANCHORED` and the pre-marked fix line in `ai.js` (`// ANCHORING: delete this line`).
- **Output:** `docs/live-check-results.md` — v0 §6's table auto-filled (both arms side by side), calibration section grouping ID accuracy by stated confidence per arm, the anchoring verdict, sold-range hit rates per arm, a pass/fail line for the ≥4-of-5 core-item gate, and an explicit `Manual remainder:` section (eBay medians if no SerpApi key; kill-the-key revocation test).
- Sequential calls, 2s apart; on any error code print it and continue. Usage header: `GEMINI_KEY=... SERPAPI_KEY=... node scripts/live-check.mjs --anchor=mug`.

**Verification:** script runs end-to-end against whatever fixtures exist (document behavior with zero fixtures: print the convention and exit 0); no key material in any output file or committed file; `npm run build`/`npm test` untouched and green; summary lists files changed.
